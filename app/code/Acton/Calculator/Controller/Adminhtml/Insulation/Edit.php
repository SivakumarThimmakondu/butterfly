<?php


namespace Acton\Calculator\Controller\Adminhtml\Insulation;

class Edit extends \Acton\Calculator\Controller\Adminhtml\Insulation
{

    protected $resultPageFactory;

    /**
     * @param \Magento\Backend\App\Action\Context $context
     * @param \Magento\Framework\Registry $coreRegistry
     * @param \Magento\Framework\View\Result\PageFactory $resultPageFactory
     */
    public function __construct(
        \Magento\Backend\App\Action\Context $context,
        \Magento\Framework\Registry $coreRegistry,
        \Magento\Framework\View\Result\PageFactory $resultPageFactory
    ) {
        $this->resultPageFactory = $resultPageFactory;
        parent::__construct($context, $coreRegistry);
    }

    /**
     * Edit action
     *
     * @return \Magento\Framework\Controller\ResultInterface
     */
    public function execute()
    {
        // 1. Get ID and create model
        $id = $this->getRequest()->getParam('insulation_id');
        $model = $this->_objectManager->create(\Acton\Calculator\Model\Insulation::class);
        
        // 2. Initial checking
        if ($id) {
            $model->load($id);
            if (!$model->getId()) {
                $this->messageManager->addErrorMessage(__('This Insulation no longer exists.'));
                /** @var \Magento\Backend\Model\View\Result\Redirect $resultRedirect */
                $resultRedirect = $this->resultRedirectFactory->create();
                return $resultRedirect->setPath('*/*/');
            }
        }
        $this->_coreRegistry->register('acton_calculator_insulation', $model);
        
        // 3. Build edit form
        /** @var \Magento\Backend\Model\View\Result\Page $resultPage */
        $resultPage = $this->resultPageFactory->create();
        $this->initPage($resultPage)->addBreadcrumb(
            $id ? __('Edit Insulation') : __('New Insulation'),
            $id ? __('Edit Insulation') : __('New Insulation')
        );
        $resultPage->getConfig()->getTitle()->prepend(__('Insulations'));
        $resultPage->getConfig()->getTitle()->prepend($model->getId() ? __('Edit Insulation %1', $model->getId()) : __('New Insulation'));
        return $resultPage;
    }
}
